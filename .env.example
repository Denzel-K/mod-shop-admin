# Database Configuration
MONGODB_URI=

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-here-make-it-long-and-random

# Application URL
NEXTAUTH_URL=http://localhost:3000
# For production: https://your-domain.com

# Email Configuration (SMTP)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
SMTP_FROM=noreply@modshop.com

# Environment
NODE_ENV=development

# Storage Provider Toggle
# Set to true to use Google Cloud Storage, false to save under /public locally
USE_GCP=false

# Google Cloud (Uploads with GCS)
# Service account credentials (JSON fields)
GCP_PROJECT_ID=your-gcp-project-id
GCP_CLIENT_EMAIL=your-service-account@your-gcp-project-id.iam.gserviceaccount.com
# IMPORTANT: paste the private key exactly as provided by Google but replace real newlines with \n when setting in .env files
GCP_PRIVATE_KEY=-----BEGIN PRIVATE KEY-----\nMIIEv...\n-----END PRIVATE KEY-----\n
# Target bucket for uploads (must exist and be publicly readable if you want public URLs)
GCS_BUCKET=your-public-bucket-name
